#include <iostream>

using namespace std;

int exampleFun(int parm)
{
	if (param > 0) {
		if (param > 100) {
			param -= 100;
			return param;
		}	
	}
	else
	{
		return 0;
	}
}

void fun()
{}

int main()
{
	// kkk
	int p = 10;
	int r = param(p);
	if (r > 0)
		cout << r;
	return 0;
}
